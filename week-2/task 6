def all_eq(lst):
    max = 0

    for s in lst:
        if len(s) > max:
            max = len(s)

    result = []

    for s in lst:
        diff = max - len(s)
        s = s + "_" * diff
        result.append(s)

    return result
print("HELLO")


